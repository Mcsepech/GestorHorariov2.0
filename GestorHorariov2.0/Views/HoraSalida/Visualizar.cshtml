@model GestorHorariov2._0.Models.HoraSalida
@{
    ViewBag.Title = "Visualizar";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="~/HoraEntrada/Index">Hora Entrada</a>
        </li>
        <li class="breadcrumb-item active">@Model.salida_hora.ToString()</li>
    </ol>
</nav>

<div class="card">
    <div class="card-header">
        Información detallada de los Hora Entrada
    </div>
    <div class="card-body">
        <dl class="dl-horizontal">
            <dt>Id</dt>
            <dd>@Model.salida_id</dd>
            <dt>Nombre</dt>
            <dd>@Model.salida_hora</dd>
            <dt>Estado</dt>
            <dd>@(Model.salida_estado == "A" ? "Activo" : "Inactivo")</dd>
        </dl>
    </div>
</div>

